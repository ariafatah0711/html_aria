import path from "path";

const file = "../script/contoh.txt";

console.info(path.dirname(file)); // direktori
console.info(path.basename(file)); // nama file
console.info(path.extname(file)); // extension name file
